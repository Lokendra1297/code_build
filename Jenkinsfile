pipline {
    
    agent any

    stages {

        stage("build") {

            steps {
                
            }
        }

        stage("deploy") {

            steps {
                
            }
        }
        
    }
}
